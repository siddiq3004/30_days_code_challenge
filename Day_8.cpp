#include <iostream>
using namespace std;
//linear probing with hashing
int main()
{
   int i,m,k,h;
   int size=200,arr[size];
   cout<<"enter the size of an array :";
   cin>>m;
   cout<<"enter the key element in to an array : ";
   cin>>k;
   int h1= k % m;
   h=((h1 + i) % (m));
   for(i=0;i<m;i++){
       if()
       cout<<h<<"at index "<<i;
   }
   

    return 0;
}

